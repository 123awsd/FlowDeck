# Codex Control Tower 项目约定

## 开始工作前

- 先阅读 `README.md` 和 `docs/PROJECT_MEMORY.md`，确认当前功能、稳定产品约束与待办事项。
- 修改某个已有子系统前，继续阅读 `docs/PROJECT_MEMORY.md` 中链接的对应专题文档。
- 文档记录的是长期意图；代码反映当前实现。如果二者冲突，先检查 Git 历史并向用户说明，不要静默选择其中一方。

## 长期产品约束

- 用户界面使用简体中文，保持紧凑、清晰、精美；避免暴露容易让旁观者误解或使用户尴尬的用途标签。
- 应用保持本地优先。项目路径、对话标题、账号信息、学习记录等个人数据不得无必要上传。
- 不在仓库文档、源码或提交中记录 API Key、密码、Cookie、Token、真实账号凭据等秘密。
- 保留现有悬浮球、窗口置顶、VS Code 窗口发现/跳转、Codex Switch、待办、系统监控等能力，除非用户明确要求移除。
- 长期运行的数据必须有容量或时间边界，不能让数据库、缓存或日志无限增长。

## 项目记忆维护

以下变更不能只留在聊天记录中，必须在同一次修改中更新 `docs/`：

- 新功能或功能定位发生变化；
- 重要 UI/交互约束；
- 数据结构、持久化、隐私或清理策略；
- 外部插件、账号、远程机器或 API 集成方式；
- 推荐、评分、状态判断等核心算法原则；
- 用户明确表示“以后都这样做”的长期偏好；
- 已确认但暂未实现、未来需要继续完成的方案。

更新时至少记录：日期、状态（计划/已实现/已废弃）、决定、原因、涉及文件和下一步。小型样式修复、拼写修正和一次性排障过程不需要单独写决策。

新增专题时：

1. 在 `docs/` 创建独立 Markdown 文档；
2. 从 `docs/PROJECT_MEMORY.md` 链接它；
3. 在专题文档中区分“已实现”和“计划”，避免把设计误写成现状；
4. 需要新 Codex 会话始终注意的约束，再用一句话补充到本文件。

## 验证与提交

- Python 修改至少运行 `python3 -m py_compile`；推荐系统修改同时运行 `python3 -m unittest -v test_learning_feed.py`。
- UI 修改需要渲染或实际启动检查布局、中文字体、裁切和交互状态。
- 提交前运行 `git diff --check`，确认 `.env`、运行数据库和个人状态文件没有进入 Git。

